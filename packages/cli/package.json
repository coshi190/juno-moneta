{
    "name": "@coshi190/juno-moneta-cli",
    "version": "0.1.0",
    "description": "1:1 command line surface over the SDK",
    "private": true,
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/coshi190/juno-moneta.git",
        "directory": "packages/cli"
    },
    "type": "module",
    "bin": {
        "juno-moneta": "./dist/cli.js"
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "rm -rf dist && tsc -p tsconfig.build.json && chmod +x dist/cli.js",
        "clean": "rm -rf dist *.tsbuildinfo",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@coshi190/juno-moneta-sdk": "file:../sdk",
        "viem": "^2.25.0"
    },
    "devDependencies": {
        "@types/node": "^22.10.0",
        "typescript": "^5.8.0"
    }
}
